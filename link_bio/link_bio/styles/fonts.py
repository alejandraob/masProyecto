import reflex as rx
from enum import Enum

class Font(Enum):
    """A class to define the fonts for the website."""
    PRIMARY = "Arial"
    SECONDARY = "Helvetica"
    LOGO = "Poppins Bold"
    BODY = "Roboto"
    DEFAULT = "Poppins Light"